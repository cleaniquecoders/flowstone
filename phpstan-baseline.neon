parameters:
    ignoreErrors:
        # Intentional runtime checks for optional trait methods
        - '#Call to function method_exists\(\) with .* will always evaluate to true#'
        # Unsafe usage of new static() in event listeners - intentional pattern
        - '#Unsafe usage of new static\(\)#'
        # Nullsafe operator simplification suggestion
        - '#Using nullsafe property access .* is unnecessary#'
